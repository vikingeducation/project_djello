import React from 'react'
import { Container } from 'mdbreact'

const BoardError = (props) => {
	return (
		<Container className="text-center">
			<h1 className="align-middle">Board Deleted!</h1>
		</Container>
	)
}

export default BoardError;