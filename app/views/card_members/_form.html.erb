<%= form_for(@card_member) do |f| %>
  <% if @card_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_member.errors.count, "error") %> prohibited this card_member from being saved:</h2>

      <ul>
      <% @card_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
